part of mind_map;

class MindMapDrawer extends StatelessWidget {
  final MindMapFileModel data;

  MindMapDrawer(this.data);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
